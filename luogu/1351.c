#include <stdio.h>

int main(void) {
	unsigned long long n, t, lst;
	unsigned long long l[200001][200000];
	scanf("%lld", &n);
	for (unsigned long long i = 1; i < n; i++) {
		unsigned long long u, v;
		scanf("%lld %lld", &u, &v);
		
	}
}
